program Hello;
var
	a,b,c: string;
	m : integer = 1;
	g, helli : real;

procedure a(t,d:integer; var a: string);
var c: integer;
var b: string;
begin
a := 5;
end;

var
	a: string;
begin
	repeat a := 5 until not (5 and 6 + a > 6 or 4) and not ((5 > 4) or (5 < 3));
	for a := 3 to 6 do
		begin
			b:= 6;
			exit;
		end;
	if (a = 5 or 7) and ((a > 6) or (a < 4)) then read(a,b) else a := 8;
	while (a = 7) do if (a = 5 or 7) and ((a > 6) or (a < 4)) then auba(5,a,10.15,'hi') else halt (5);
end.